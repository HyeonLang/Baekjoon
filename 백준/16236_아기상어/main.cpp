#include <iostream>
#include <vector>
using namespace std;

int N;
int space[20][20];

bool fishCheck(int x, int y, ) {
    space[y][x]
}

int nextFind(int now_x, int now_y) {
    int x = now_x;
    int y = now_y;
    int count = 1;
    while (true) {
        for (int i = 0; i < count + 1; i++) {
            x = now_x - count + i;
            y = now_y - i;
            fishCheck()

        }
    }
}

int main() {
    cin >> N;
    int shark[2];
    vector<vector<int>> fish;

    return 0;
}
